/**
 * Serviço principal da aplicação responsável pela lógica de negócios.
 * 
 * O `AppService` contém métodos que implementam a lógica do aplicativo. 
 * Neste caso, o serviço inclui um método que retorna uma mensagem de saudação.
 * 
 * Métodos:
 * - `getHello()`: Retorna uma mensagem de saudação.
 */

export declare class AppService {
    getHello(): string;
}
